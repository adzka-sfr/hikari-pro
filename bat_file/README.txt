Catatan
    1. untuk file bat / batch connection (ORACLE) tidak bisa dari luar / include, harus jadi satu sama file bat nya
    2. untuk sekedar view connection (ORACLE) bisa dari luar / include
    3. untuk file bat / batch koneksi (mysql) bisa dari luar